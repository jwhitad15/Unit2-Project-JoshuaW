spring.application.name=FOTHS

spring.datasource.url=${DB_URL}

#when using localhost:8080, remember with the default login page with username: 'user' and the auto-generated password printed in the console.
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
#spring.jpa.hibernate.ddl-auto=none
spring.jpa.hibernate.ddl-auto=update
spring.main.allow-bean-definition-overriding=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

jwt.secret=ForWeWalkByFaith,NotBySight_2Cor5:7
jwt.expirationMs=86400000

spring.security.user.name=admin
spring.security.user.password=password

logging.level.org.springframework.web=DEBUG


# Application properties includes properties for the Spring Boot application, like the application name,
# database connection details, and JPA settings.

#Deploy Ready